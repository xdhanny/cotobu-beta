<div class="page-header">
  <div class="row">
    <div class="span3"> 
      <h1>Resorts</h1>
    </div>
    <div class="span2 btn">
        <!--<%= button_to 'Crea un nuevo Destino', new_place_path %>-->
        <%= link_to 'New Resort', new_place_path %>
    </div>
  </div>
</div>
  <!-- Antigua tabla que muestra CRUD
<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @places.each do |place| %>
  <tr>
    <td><%= place.name %></td>
    <td><%= place.description %></td>
    <td><%= link_to 'Show', place %></td>
    <td><%= link_to 'Edit', edit_place_path(place) %></td>
    <td><%= link_to 'Destroy', place, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>
 Fin de antigua tabla -->

<br />

<ul class="thumbnails">
  <%= render  :partial => @places %>
</ul>
